function LearnButton(){
    return (
        <>

            {/*<Button type="primary" loading={false} size="large" onClick={handleClick} danger={true}>nội dung</Button>*/}
            {/*<Button icon={<MessageOutlined rotate={90} spin={true} />}>button</Button>*/}
        </>
    )
}
export default LearnButton;